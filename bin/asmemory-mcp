#!/usr/bin/env python3
"""asmemory MCP server 可执行入口（零依赖）。"""
import os
import sys

sys.path.insert(0, os.path.join(os.path.dirname(os.path.abspath(__file__)), "..", "src"))

from asmemory.server import main  # noqa: E402

main()
